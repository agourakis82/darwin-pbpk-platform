apiVersion: apps/v1
kind: Deployment
metadata:
  name: darwin-pbpk-platform
  namespace: darwin-pbpk-platform
  labels:
    app: darwin-pbpk-platform
    version: v2.0.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: darwin-pbpk-platform
  template:
    metadata:
      labels:
        app: darwin-pbpk-platform
        version: v2.0.0
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: darwin-pbpk-platform
        image: ghcr.io/agourakis82/darwin-pbpk-platform:v2.0.0
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        - name: grpc
          containerPort: 50051
          protocol: TCP
        - name: metrics
          containerPort: 9090
          protocol: TCP
        env:
        - name: DARWIN_CLUSTER_ENABLED
          value: "true"
        - name: DARWIN_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: DARWIN_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: DARWIN_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: PYTHONUNBUFFERED
          value: "1"
        envFrom:
        - secretRef:
            name: darwin-pbpk-platform-secrets
        - configMapRef:
            name: darwin-pbpk-platform-config
        resources:
          requests:
            memory: "4Gi"
            cpu: "2000m"
            nvidia.com/gpu: 1
          limits:
            memory: "16Gi"
            cpu: "8000m"
            nvidia.com/gpu: 1
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: cache
          mountPath: /app/.cache
      volumes:
      - name: config
        configMap:
          name: darwin-pbpk-platform-config
      - name: cache
        emptyDir: {}
      restartPolicy: Always

