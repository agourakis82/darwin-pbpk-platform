apiVersion: v1
kind: Service
metadata:
  name: darwin-pbpk-platform
  namespace: darwin-pbpk-platform
  labels:
    app: darwin-pbpk-platform
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: darwin-pbpk-platform
---
apiVersion: v1
kind: Service
metadata:
  name: darwin-pbpk-platform-headless
  namespace: darwin-pbpk-platform
  labels:
    app: darwin-pbpk-platform
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
  selector:
    app: darwin-pbpk-platform

